{
  "name": "db",
  "version": "1.0.0",
  "private": true,
  "description": "Wishmasters Spot-the-Ball Database Package with Prisma",
  "main": "index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:reset": "prisma migrate reset",
    "studio": "prisma studio",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "prisma": "^6.2.0",
    "tsx": "^4.20.6",
    "typescript": "^5.7.3",
    "@types/node": "^20.17.10",
    "@types/bcrypt": "^5.0.2"
  }
}
