{
  "name": "db",
  "version": "1.0.0",
  "private": true,
  "description": "Goldmasters Spot-the-Ball Database Package with Prisma",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm run generate && tsc && node copy-prisma.js",
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:reset": "prisma migrate reset",
    "studio": "prisma studio",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20.17.10",
    "prisma": "^6.2.0",
    "tsx": "^4.20.6",
    "typescript": "^5.7.3"
  }
}
